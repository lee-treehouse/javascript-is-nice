// no loops probably constant time O(1)

// if there is a loop, or more, but not nested it's probably O(n) ie linear

// if there is a nested loop it's not linear eg quadratic

// for O(sqrt n) is better than linear but kindof subset of O(n)
